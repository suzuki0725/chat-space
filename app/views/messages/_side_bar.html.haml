.side-bar
  .side-bar__user-name
    .side-bar__user-name__top-item
      .top-item__name 
        =current_user.name
      .top-item__icons
        =link_to new_group_path, class: "icons__btn-edit" do
          =icon('far', 'edit')
        =link_to edit_user_path(current_user), class: "icons__btn-cog" do
          =icon('fas', 'cog') 
  .side-bar__group-list
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .side-bar__group-list__group-name
            = group.name
          .side-bar__group-list__latest-text
            メッセージはまだありません。